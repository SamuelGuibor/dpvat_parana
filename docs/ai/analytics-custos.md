# Dashboards, métricas e custos — mapa para IA
> Verificado em 2026-09-23 · Escopo: `app/_actions/analytics/**`, `app/_actions/costs/**`, `app/_actions/maintenance/**`, `app/nova-dash/StrategicDashboard.tsx`, `app/nova-dash/DateFilter.tsx`, `app/nova-dash/{KanbanFlowPanel,form-leads,FolderReport}.tsx`, `app/nova-dash/workspace/manager/**`, `app/nova-dash/workspace/costs/**`, `app/_shared/lib/{costs,cost-providers,cost-sync,ai-pricing,meta-conversions,google-sheets}.ts`, `app/_shared/utils/date-br.ts`, `app/api/{costs,logs,maintenance}`, `tests/{costs,date-br}.test.ts`

## TL;DR
- Três telas do Espaço de Trabalho da nova-dash (`app/nova-dash/workspace/Workspace.tsx`): **Dashboard / Gestão Estratégica** (`StrategicDashboard`: funil do bot, Fluxo de Eventos Rápidos, Origem dos leads, Fluxo do Kanban, aba Chatbot com Canto da IA; também montado direto em `app/nova-dash/page.tsx`), **Visão do Gestor** (`ManagerDashboard`: ranking/heatmap da equipe + aba de Setores) e **Custos** (`CostsPanel`: consumo por serviço, projeção do mês, crédito pré-pago e faturas manuais).
- Não existe tabela de agregados: toda métrica é recalculada na hora por server action a partir de `Log`, `WhatsAppConversation`/`Tag`/`Message`/`Contact` e `Botconversa` (legado). Custos vêm de `cost_snapshots` (sync diário) + `project_costs` (manual).
- O que mais quebra: (1) cortar dia/mês fora de `date-br.ts` (servidor roda em UTC); (2) criar KPI com uma definição própria de "contratado/qualificado" em vez de `loadCohort`; (3) chamada de IA que não grava `metadata.usage` some do Canto da IA e dos custos.
- **Os crons `/api/costs/sync` e `/api/maintenance/retention` estão fora de `PUBLIC_API_PREFIXES` em `middleware.ts`**: o Vercel Cron chega sem cookie e leva 401 do middleware antes do check de `CRON_SECRET` (confere com "cost sync parado desde 14/09"). Só o botão "Atualizar agora" sincroniza hoje.

## Onde fica
| Arquivo | Responsabilidade | Símbolos-chave |
|---|---|---|
| `app/_actions/analytics/get-strategic-dashboard.ts` | Carga ÚNICA do dashboard (Promise.all de tudo) | `getStrategicDashboardData`, `StrategicDashboardData` |
| `app/_actions/analytics/bot-funnel.ts` | Funil do bot + Fluxo de Eventos Rápidos por coorte; meta mensal | `getBotFunnel`, `getBotKanbanLeads`, `setMonthlyHiredGoal`, `BotStage`, `loadCohort` (interno) |
| `app/_actions/analytics/get-funnel-analytics.ts` | Fluxo do Kanban pelos logs `move`; meta legada (`Goal`) | `getKanbanFlowAnalytics`, `getFunnelAnalytics`, `getMonthGoal`, `setMonthGoal`, `EXPECTED_FLOWS`, `DONE_COLUMN_RE`, `DISCARD_COLUMN_RE` |
| `app/_actions/analytics/get-chatbot-analytics.ts` | Desempenho do chatbot (logs `wa_*`), origem de anúncios, desfechos | `getChatbotAnalytics`, `getAdLeadOutcomes`, `getChatbotDashboardAccess`, `getLeadFunnel` (sem uso na UI) |
| `app/_actions/analytics/get-ai-corner.ts` | Extrato de gasto de IA (mês corrente × 30 dias) | `getAiCorner`, `OPERATION_LABELS`, `OPERATION_ICONS` |
| `app/_actions/analytics/get-team-analytics.ts` | Ranking + heatmap 7×24 da equipe (Log.authorId) | `getTeamAnalytics` |
| `app/_actions/analytics/get-collaborator-detail.ts` | Drill-down de um colaborador (janela máx. 90d) | `getCollaboratorDetail` |
| `app/_actions/costs/index.ts` | CRUD das faturas manuais (`project_costs`) | `listProjectCosts`, `createProjectCost`, `updateProjectCost`, `deleteProjectCost` |
| `app/_actions/costs/overview.ts` | Visão do mês por serviço, projeção, crédito | `getCostOverview`, `syncCostsNow`, `setServiceCredit` |
| `app/_actions/maintenance/retention.ts` | Purga de `Notification` e logs `wa_*` antigos | `runRetention`, `purgeOldNotifications`, `purgeOldLogs`, `PURGEABLE_LOG_ACTIONS` |
| `app/_shared/lib/costs.ts` | Catálogo de serviços e dinheiro (client+server) | `COST_SERVICES`, `COST_PROVIDER_INFO`, `parseMoneyToCents`, `formatMoney`, `formatMonthLabel` |
| `app/_shared/lib/cost-providers.ts` | Um fetcher por provedor → custo diário | `fetchAllProviders`, `anthropicCosts`, `aiCostsFromLogs`, `geminiCosts`, `neonCosts`, `railwayCosts`, `metaAdsCosts`, `whatsappCosts`, `awsCosts`, `vercelCosts` (stub), `fetchUsdBrl` |
| `app/_shared/lib/cost-sync.ts` | Grava snapshots + status + câmbio | `runCostSync`, `readSyncStatus`, `readFx`, `COST_SYNC_STATUS_KEY`, `COST_FX_KEY` |
| `app/_shared/lib/ai-pricing.ts` | Preço por modelo e custo por chamada (fonte única) | `MODEL_PRICING`, `priceFor`, `usageCostUSD`, `usageTokens`, `modelLabel` |
| `app/_shared/lib/meta-conversions.ts` | CAPI da Meta: estágio do lead (qualificado/não) | `reportLeadStageToMeta`, `sendMetaCrmEvent`, `META_LEAD_STAGES` |
| `app/_shared/lib/google-sheets.ts` | Append de linha em planilha (service account JWT) | `appendSheetRow`, `sheetsConfigured`, `extractSpreadsheetId` |
| `app/_shared/utils/date-br.ts` | Fuso de Brasília — única fonte de "que dia é hoje" | `BR_TZ`, `brDayKey`, `brStartOfDay`, `brStartOfDaysAgo`, `brDayKeySeries`, `brStartOfMonth`, `brMonthIndex`, `brDaysInMonth`, `brDayOfMonth`, `brLabelFromKey`, `brLocalToDate`, `brDateTimeParts` |
| `app/nova-dash/StrategicDashboard.tsx` | Tela do dashboard; seletor global de número + `DateFilter` | `StrategicDashboard` |
| `app/nova-dash/DateFilter.tsx` | Presets/calendário (roda no navegador) | `DateFilter`, `getDefaultDateRange`, `DateRange` |
| `app/nova-dash/KanbanFlowPanel.tsx` | UI do Fluxo do Kanban; exporta CSV (`;` + BOM) e JPEG do gráfico com helpers LOCAIS | `downloadBlob`, `exportCsv`, `exportJpeg` |
| `app/nova-dash/form-leads.tsx` | Aba "Leads" do dashboard (formulário do site via `getContacts`) + botão "Exportar CSV" local | `LeadsTable` |
| `app/nova-dash/workspace/manager/*` | UI: `BotFunnelSection`, `ChatbotDashboard`, `AiCorner`, `LeadOriginSection`, `ManagerDashboard`, `CollaboratorDetail`; `SystemMap` está comentado (morto) | — |
| `app/nova-dash/workspace/costs/*` | UI: `CostsPanel` (recharts), `CostFormDialog` | — |
| `app/api/costs/sync/route.ts` | Cron 03:30 UTC (`vercel.json`), `?days=` 1..62 (padrão 3), `maxDuration` 120 | `GET` |
| `app/api/maintenance/retention/route.ts` | Cron 05:00 UTC, `maxDuration` 300 | `GET` |
| `app/api/logs/route.ts` | Histórico de logs de um card (take 200, só equipe) | `GET` |

## Fluxo principal
**Dashboard (Gestão Estratégica)**
1. `StrategicDashboard` monta com `getDefaultDateRange()` (mês corrente no fuso do NAVEGADOR) e manda `from/to` em ISO.
2. `getStrategicDashboardData(fromISO, toISO, monthKey)` → `requireTeam()` → Promise.all: `fetchEventsCount`/`fetchEventsByMonth`/`fetchBotconversaAll` (legado, `app/_shared/lib/db/botconversa.ts`), `getContratadosTagCount`, `getMonthGoal`, `getFunnelAnalytics`, `getKanbanFlowAnalytics` e, se `getChatbotDashboardAccess()`, `getChatbotAnalytics(7, null, from, to)` + `listWaNumberOptions`. A UI só usa `kanban`, `kanbanFlow` e `chatbot`; o resto é carga morta.
3. Em paralelo e por conta própria: `BotFunnelSection` → `getBotFunnel(period, numberId, from, to)`; `getBotKanbanLeads` → `MiniKanban` (só `evento === 'contratado'` do legado + leads do sistema); `LeadOriginSection` → `getChatbotAnalytics`/`getAdLeadOutcomes`; `KanbanFlowPanel` rechama `getKanbanFlowAnalytics` para "comparar com período anterior".
4. `getBotFunnel` → `loadCohort(numberId, since, until)` classifica cada conversa em UMA etapa (prioridade: contratado > nao_qualificado > nao_contratado > enviou_documentos > iniciado > em_conversa > outros) + contagens do mês/ano de referência (= FIM do range).

**Custos**
1. `CostsPanel` → `getCostOverview()` (snapshots desde o 1º dia do mês anterior + `project_costs` + `cost_credit_*`) e `listProjectCosts(from,to)`.
2. "Atualizar agora" → `syncCostsNow(35 na 1ª vez, senão 3)` → `runCostSync(days)`; o cron faria o mesmo via `/api/costs/sync`.
3. `runCostSync` → `fetchUsdBrl` (env `COST_USD_BRL` > awesomeapi > fallback 5.1) grava `cost_fx_usd_brl` → `fetchAllProviders(range, fx)` (cada provedor falha sozinho) → upsert `CostSnapshot` por `service_day` → grava `cost_sync_status`.
4. `getCostOverview` converte USD→BRL, soma mês corrente/anterior, média dos últimos 14 dias com registro, projeção e dias de crédito.
5. Crédito pré-pago: `setServiceCredit` grava `cost_credit_<service>` (valor 0 apaga); saldo = crédito − consumo desde `setAt`, na moeda do crédito; `daysLeft` = saldo ÷ média diária.

**Canto da IA** (aba Chatbot)
1. Aba Chatbot do `StrategicDashboard` → `ChatbotPanel` (`app/nova-dash/workspace/chatbot/ChatbotPanel.tsx`, recebe `initialAnalytics` da carga única) → `ChatbotDashboard` → `AiCorner` → `getAiCorner()` (allowlist por e-mail).
2. Uma única `$queryRaw` em `logs` com `jsonb_exists(metadata,'usage')` desde o menor entre o 1º do mês (BRT) e 30 dias atrás.
3. `buildWindow` agrega por `action` em três janelas: mês corrente (bate com o console da Anthropic), últimos 30 dias e hoje; projeção = média diária do mês × dias do mês; `costPerBotDecision` = gasto do mês ÷ execuções de `wa_bot`.

**Retenção**: cron → `runRetention()` → `purgeOldNotifications()` (lidas > 30d, todas > 90d) + `purgeOldLogs()` (`PURGEABLE_LOG_ACTIONS` > 180d).

## Dados
- **`Log`** (`logs`): `action` + `metadata` Json. Semânticas usadas aqui: `move` → `{from, to, cardName}` (Fluxo do Kanban); `wa_*` → `metadata.contactId` (é o único jeito de filtrar log por número — log não tem `numberId`); `wa_bot` → `outcome`, `intent`, `understood`, `confidence`, `durationMs`; qualquer ação de IA → `metadata.usage {model, inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens}`; `dev_commit` → `metadata.files`; `wa_account` = aviso da Meta. Índice `[action, createdAt]`.
- **`WhatsAppConversation`**: `status` bot|queued|human|standby|closed; `closeCategory` (qualificado, nao_qualificado, `nq_*`, sem_resposta, perguntas, transferido, descartado, novo_acidente, contratado_perdido); `closedAt` = último encerramento, **só vale com status closed** (reabertura não limpa); `botState` null = IA não avançou etapa ("Iniciado"); `qualified`; `numberId`.
- **`WhatsAppConversationTag.createdAt`** = data em que a tag foi APLICADA (é a régua de "contratado no período"); linhas anteriores a 03/08/2026 herdaram a data da migration. PK `[conversationId, tagId]`: remover e reaplicar "Contratados" cria linha nova e muda o contratado de mês.
- **`WhatsAppContact`**: `optInSource = 'inbound'` separa lead real de contato "fantasma" criado por aviso do kanban; `adPlatform/adHeadline/adSourceId/adSourceUrl` (origem CTWA); `metaLeadStage/metaLeadStageAt` (dedupe do CAPI).
- **`WhatsAppMessage`**: "Lista docs" = mensagem `out`, não interna, contendo `DOCS_FINGERPRINT = 'RG ou da sua CNH'`.
- **`WhatsAppCloseReason`**: sub-motivos `nq_*` editáveis pela equipe (lista não é fixa).
- **`Botconversa`**: legado; só `evento = 'contratado'` (por `updatedAt`) ainda entra em contratados/meta, e só na visão "todos os números".
- **`BotKanbanLead`** (linha da coorte que `loadCohort` devolve e `getBotKanbanLeads` expõe) = `{id, nome, telefone, evento, createdAt, updatedAt, numberLabel}`. NÃO traz se a conversa é "Qualificada" (`loadCohort` só devolve o total `qualified`) nem flag de "criada no período" (`getBotFunnel` recalcula `createdInPeriod` comparando `createdAt` com o range). Lista/export que precise bater com os KPIs "Qualificados"/"Total no período" → estenda `loadCohort` com esses campos por linha, não reconte no cliente.
- **"Total no período" (`started`) ≠ nº de linhas da coorte**: a coorte também traz conversas antigas com a tag "Contratados" aplicada no período (contam em Contratado, não no total); e o KPI Contratados (`hired`) soma `hiredLegacy` (contagem de `Botconversa`, sem linha na coorte — no MiniKanban o legado chega à parte, via `kanban` de `getStrategicDashboardData`).
- **`CostSnapshot`** (`cost_snapshots`): único `[service, day]`; `day` "YYYY-MM-DD" (o comentário do schema diz "Brasília", mas quase todo provedor grava dia UTC — ver regras); `amountCents` na moeda gravada (`currency` USD|BRL; Meta Ads já chega convertido para BRL pelo câmbio do sync); `source` api|logs|estimate; `detail` Json (ex.: `spread: true` no Neon); provedor que falha mantém os snapshots antigos.
- **`ProjectCost`** (`project_costs`): fatura manual; `chargedAt` gravado ao **meio-dia UTC**; `amountBrlCents` é o que soma nos totais.
- **`Goal`** (`goals`, `[month, metric]`, metric "contratos"): meta LEGADA, ainda lida por `getMonthGoal` mas não exibida. A meta viva é `AppSetting` `monthly_hired_goal` (default 60).
- **`AppSetting`** chaves do domínio: `monthly_hired_goal`, `cost_sync_status` (JSON por serviço), `cost_fx_usd_brl`, `cost_neon_cumulative` (acumulado anterior do Neon), `cost_credit_<service>` (`{amountCents, currency, setAt, note}`).
- **Strings mágicas**: tags exatas `'Contratados'` e `'Qualificada'` (`HIRED_TAG`, `QUALIFIED_TAG`); `DISCARD_COLUMN_RE = /nikolas/i`; `DONE_COLUMN_RE` (agendar perícia adm | fazer roteiro prev); `EXPECTED_FLOWS` com nomes literais de colunas; `EXCLUDED_ACTIONS = ['wa_transcribe']`; `DEV_COMMIT_ACTION = 'dev_commit'`.
- **Variáveis de ambiente** (só nomes): custos → `ANTHROPIC_ADMIN_KEY` (sem ela, Claude é estimado pelos logs), `ANTHROPIC_COST_UNIT=usd` (se o cost_report não bater; padrão lê centavos), `NEON_API_KEY`, `NEON_PROJECT_ID`, `NEON_PRICE_*`, `RAILWAY_API_TOKEN`, `RAILWAY_PROJECT_ID`, `RAILWAY_PRICE_*`, `META_ADS_ACCOUNT_ID`, `META_ADS_TOKEN` (cai em `META_CONVERSIONS_TOKEN`), `META_ADS_ACCOUNT_BOTCONVERSA`, `META_ADS_TOKEN_BOTCONVERSA`, `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` (precisa de `ce:GetCostAndUsage`), `COST_USD_BRL` (fixa o câmbio), `CRON_SECRET`; CAPI → `META_DATASET_ID`, `META_CONVERSIONS_TOKEN` (só envio, sem leitura), `META_TEST_EVENT_CODE`; Sheets → `GOOGLE_SHEETS_CLIENT_EMAIL`, `GOOGLE_SHEETS_PRIVATE_KEY`; acesso → `CHATBOT_DASHBOARD_EMAILS`, `MANAGER_EMAILS`.
- Vercel e Gemini não têm API de gasto aqui: Vercel é sempre fatura manual; Gemini só aparece se algum log gravar `usage` com modelo `gemini*`.
- Cards do kanban são linhas de `User` (tabela `Process` vazia em produção) → no Fluxo do Kanban a chave do card é `u:<userId>` (`p:<processId>` só se o log tiver `processId`).

## Regras invioláveis e armadilhas
- **Todo corte de dia/mês/hora no servidor passa por `date-br.ts`** — nunca `toISOString().slice(0,10)`, `getDate()`, `getMonth()`, `setHours(0,0,0,0)`. Vercel roda em UTC: das 21h BRT em diante já é "amanhã". Heatmaps usam `Intl` com `America/Sao_Paulo`. Exceção consciente: `DateFilter`, `periodRange` (CostsPanel.tsx) e `currentMonthKey` (StrategicDashboard.tsx) rodam no navegador e só enviam ISO.
- **KPI novo do funil do bot nasce de `loadCohort`** (bot-funnel.ts): coorte = conversas criadas no período OU com tag Contratados aplicada no período, cada uma em uma etapa só. Contagens soltas davam Funil × Fluxo que nunca batiam.
- **Contratado = tag exata "Contratados" aplicada no período** (+ legado Botconversa na visão geral) — card e meta do mês usam a mesma régua. `getContratadosTagCount` (`app/_actions/whatsapp/tags.ts`) usa `contains 'contratad'`, definição diferente: não reutilize em KPI novo.
- **Não qualificado = `nao_qualificado` OU `startsWith('nq_')`**; nunca liste `nq_*` fixos em métrica (são editáveis) — nem use `NON_QUALIFIED_CATEGORIES` (`close-categories.ts`), que é lista fixa. Mas `META_LEAD_STAGES` também é lista fixa: `nq_*` novo NÃO vai para a Meta até entrar lá.
- **Desfecho por data usa `closedAt` com `status: 'closed'`**, não `updatedAt` (tag/nota posterior "reencerrava" no mês errado).
- **Toda chamada de IA grava `metadata.usage`** no Log. O Canto da IA (`jsonb_exists(metadata,'usage')`) e `aiCostsFromLogs` (`metadata ? 'usage'`) somam sozinhos, sem lista de ações. Preço só em `MODEL_PRICING`; `priceFor` casa por `startsWith` na ordem das chaves → chave mais específica ANTES da genérica (ex.: `gemini-2.5-flash-lite` antes de `gemini-2.5-flash`). Modelo desconhecido cai em `claude-sonnet-5` com `known:false` (UI marca "estimado"). Cache: leitura 0,1×, escrita 1,25× (TTL 5 min).
- **Dinheiro é centavo Int.** `parseMoneyToCents` arredonda na string (float perde centavo); `parseChargedAt` grava meio-dia UTC para o mês não escorregar.
- **`getCostOverview` converte todo o histórico USD com o câmbio ATUAL** (`readFx`), não o do dia; se o último sync caiu no fallback, o painel fica em 5,1. Projeção = mês até hoje + média dos últimos 14 dias COM registro × dias restantes; sem snapshots, projeção = fatura manual do mês.
- **Provedor sem chave devolve `configured:false` (não é erro)**; erro lança e vira `cost_sync_status[service].error`. Nunca deixe um provedor derrubar os demais (`fetchAllProviders` embrulha cada job). WhatsApp soma por WABA e falha por WABA.
- **Neon só expõe o acumulado do período**: o delta contra `cost_neon_cumulative` vira o dia de hoje; a 1ª leitura do período espalha por igual (`detail.spread`). Apagar essa chave refaz o espalhamento.
- **Dias dos provedores não são todos BRT**: Anthropic (cost_report)/Neon/Railway/AWS/WhatsApp usam dia UTC (`isoDay`/`dayOfIso` locais do cost-providers.ts), Meta Ads usa o dia da conta, só os logs de IA usam `brDayKey`. Railway é consultada DIA A DIA em série: `?days` alto pode estourar os 120 s da rota.
- **Crons fora do middleware**: para `/api/costs/sync` e `/api/maintenance/retention` rodarem pelo Vercel Cron é preciso incluir o prefixo em `PUBLIC_API_PREFIXES` (`middleware.ts`); a rota já valida `CRON_SECRET` (Bearer ou `?secret=`).
- **Três travas de acesso diferentes — não misture**: `requireTeam` (dashboard, funil, Fluxo do Kanban); allowlist por e-mail `canViewChatbotDashboard` (lista fixa em `app/_shared/lib/chatbot-access.ts`; env `CHATBOT_DASHBOARD_EMAILS` só SOMA) para `getChatbotAnalytics`, `getAiCorner`, `getAdLeadOutcomes`, `getLeadFunnel`; permissão `manager_dashboard` (+ `isManager`/env `MANAGER_EMAILS`) para `getTeamAnalytics`, `getCollaboratorDetail`, `setMonthlyHiredGoal`, `setMonthGoal`; `view_costs` (ADMIN++ por padrão) para tudo de custos. Armadilha: `LeadOriginSection` fica na aba Analytics (visível a toda a equipe), mas chama `getChatbotAnalytics` — quem está fora da allowlist vê a caixa de erro.
- **Dado pessoal já vai ao navegador de toda a equipe — permissão de export não protege o dado**: `getBotKanbanLeads` só exige `requireTeam` e devolve a coorte inteira com `nome` e `telefone` (MiniKanban na aba Analytics do `StrategicDashboard`); `getStrategicDashboardData` idem com `kanban` = `fetchBotconversaAll(range)` inteiro (todos os eventos; o filtro `contratado` é só no cliente). Esconder um botão de CSV atrás de permissão não impede copiar do DevTools: restrinja na server action (`requirePermission`) ou tire os campos do payload.
- **Bug latente no `exportCsv` do `KanbanFlowPanel.tsx`**: `String(v).replace('.', ',')` roda em QUALQUER célula, não só em número — troca o 1º ponto de textos (nome de card/coluna/hospital, e-mail se um dia entrar: "Hosp. X" → "Hosp, X") e deixa os demais. Correção: vírgula decimal só quando `typeof v === 'number'`.
- **Lápis da meta mensal aparece para todos** (`BotFunnelSection.tsx` não usa `usePermissions`): `setMonthlyHiredGoal` exige `manager_dashboard`; sem ela, `saveGoal` mostra o valor novo e no `catch` reverte sem toast nem mensagem — parece falha de salvar. Ao mexer: esconder com `usePermissions().perms.manager_dashboard` e avisar o erro.
- **Retenção**: nunca inclua `move` em `PURGEABLE_LOG_ACTIONS` (`app/_shared/lib/folder-report.ts` lê `move` sem limite de data) nem ações de histórico do card. Efeito colateral: métricas do chatbot e custo de IA por logs (`wa_bot`, `wa_suggest`, `wa_summary`, `wa_ficha_ai`) só enxergam 180 dias; `ai_audit`/`roteiro_ai` não são purgados.
- **Filtro por número**: conversa/mensagem/contato têm `numberId`; logs são filtrados resolvendo `metadata.contactId` → `WhatsAppContact.numberId` (log sem contactId some da visão por número).
- **Atividade da equipe**: exclua `wa_transcribe`; `dev_commit` pesa pelo nº de arquivos (`devCommitFiles`, `devFilesDelta` em `app/_shared/lib/dev-activity.ts`); só autores com role ADMIN/ADMIN+/ADMIN++ atuais entram.
- **Tetos de consulta**: `getFunnelAnalytics`/`getKanbanFlowAnalytics` têm `take: 20_000` em ordem ASC → período enorme corta os movimentos MAIS RECENTES; `getChatbotAnalytics` e `loadCohort` não têm teto (intencional). `distinct` do Prisma não vira `DISTINCT ON` (puxa tudo e deduplica em JS) — em tabela grande use SQL; o próprio `loadCohort` faz isso nas mensagens com a lista de docs (`distinct: ['contactId']`).
- **Fluxo do Kanban depende de nomes literais de coluna** (`EXPECTED_FLOWS`, `DONE_COLUMN_RE`, `DISCARD_COLUMN_RE`): renomear coluna no board quebra a classificação sem erro.
- **Arquivo `"use server"` só exporta função async** — constantes compartilhadas moram em `app/_shared/lib/costs.ts`. `runRetention`/`purge*` são exports de `"use server"` sem checagem de sessão: não importe em componente cliente.

## Receitas
- **Nova etapa/KPI no funil do bot** → `BotStage` + ordem de prioridade em `loadCohort`, `BotFunnelData` e `getBotFunnel` (bot-funnel.ts), array `stages` em `app/nova-dash/workspace/manager/BotFunnelSection.tsx`, colunas em `app/nova-dash/minikanban.tsx` · cuidado: etapas exclusivas (soma = coorte); "Qualificados" é o único KPI não exclusivo · valide: soma das barras = coluna do Fluxo de Eventos Rápidos no mesmo período/número.
- **Nova chamada de IA entrar no gasto** → gravar `metadata.usage` no log (via `createLog`/`logWhatsAppEvent` de `app/_shared/lib/log.ts`; ação nova entra na união `LogAction`); modelo novo em `MODEL_PRICING`; rótulo/ícone em `OPERATION_LABELS`/`OPERATION_ICONS` (get-ai-corner.ts) · cuidado: ordem das chaves do `startsWith` · valide: operação aparece no Canto da IA sem "estimado".
- **Novo serviço de custo** → `COST_SERVICES` + `COST_PROVIDER_INFO` (costs.ts), fetcher em cost-providers.ts registrado em `fetchAllProviders` · cuidado: devolver `configured:false` sem chave, centavos na moeda do provedor · valide: "Atualizar agora" no painel e o status do serviço em `cost_sync_status`.
- **Religar os crons de custo/retenção** → `PUBLIC_API_PREFIXES` em `middleware.ts` · cuidado: não abrir outras rotas junto · valide: `GET /api/costs/sync?days=3` com `Authorization: Bearer $CRON_SECRET` responde 200 e `cost_snapshots.fetchedAt` avança.
- **Gráfico/agrupamento por dia, semana ou mês** → funções de `date-br.ts` (`brDayKeySeries` preenche dias vazios com zero) · valide: `tests/date-br.test.ts` + conferir o último ponto às 22h BRT.
- **Coluna terminal/descarte nova no board** → `EXPECTED_FLOWS`, `DONE_COLUMN_RE`, `DISCARD_COLUMN_RE` em get-funnel-analytics.ts · valide: aba "Fluxo do Kanban" (throughput e fora do padrão).
- **Novo sub-motivo `nq_*`** → métricas pegam sozinhas; se deve virar evento na Meta, adicionar em `META_LEAD_STAGES`; rótulo em `CLOSE_CATEGORY_LABELS` (`app/_shared/lib/whatsapp/close-categories.ts`).
- **Meta mensal de contratados** → `setMonthlyHiredGoal` (AppSetting `monthly_hired_goal`); `Goal`/`getMonthGoal`/`setMonthGoal` é legado · cuidado: meta e série "Mensal" seguem o FIM do range; o lápis aparece para todos, mas só grava com `manager_dashboard` (sem aviso quando falha).
- **Exportar relatório/CSV** → não existe utilitário de CSV compartilhado nem permissão de export: hoje são 4 cópias independentes — `exportCsv`/`downloadBlob` (+ `exportJpeg`) em `app/nova-dash/KanbanFlowPanel.tsx` (aba Fluxo do Kanban, toda a equipe); `exportCsv` do `LeadsTable` em `app/nova-dash/form-leads.tsx` (aba Leads, toda a equipe; `getContacts` não checa nada além da sessão do middleware); `exportCsv` em `app/nova-dash/FolderReport.tsx` (só protegido pela permissão de VER: `view_pagos_caique`/`view_pagos_uni` em `getCaiqueFolders`/`getUniFolders`); CSV do ponto montado no servidor (`csvCell`/`csvResponse` em `app/api/work-session/route.ts`, atrás de `canManagePonto`). Padrão comum: `;` + BOM + CRLF, aspas dobradas. Utilitário novo → função pura em `app/_shared/utils/` (montar a string sem DOM; o download fica no componente) · cuidado: vírgula decimal só em `number` (bug acima); dado sensível se protege na action que o entrega, não no botão; permissão de export nova = `PERMISSION_DEFS` + `ROLE_DEFAULTS` + `requirePermission` no servidor · valide: abrir no Excel pt-BR (acentos, colunas, texto com ponto/aspas/`;`) + teste vitest do utilitário.
- **Métrica nova na Visão do Gestor** → `getTeamAnalytics`/`getCollaboratorDetail` · cuidado: restringir à equipe atual, excluir `wa_transcribe`, aplicar peso de `dev_commit`, exigir `manager_dashboard`.
- **Enxugar a carga do dashboard** → remover de `getStrategicDashboardData` o que a UI não lê (`counts`, `monthly`, `contratadosBot`, `monthGoal`, `funnel`) · cuidado: tipos exportados `StrategicCounts`/`MonthlyRow`; essa carga morta ainda usa `range.from.getFullYear()` no servidor (UTC) — não copie o padrão · valide: `npx tsc --noEmit`.
- **Ação de log nova que pode ser purgada** → só entra em `PURGEABLE_LOG_ACTIONS` se nenhuma tela lê sem limite de data.

## Testes e validação
- `tests/date-br.test.ts`: dia/mês em BRT com instantes UTC fixos (22:39 BRT, meia-noite = 03:00Z, dia 31 às 22h). Passa em qualquer TZ da máquina; para simular produção: `TZ=UTC npx vitest run tests/date-br.test.ts`.
- `tests/costs.test.ts`: `parseMoneyToCents` (vírgula/ponto, arredondamento), `formatMoney`, `formatMonthLabel`, `costServiceLabel`/`costServiceColor`.
- Sem teste para bot-funnel, analytics, cost-providers, overview e retention — valide manualmente.
- Comandos (os mesmos do CI em `.github/workflows/ci.yml`): `npx tsc --noEmit`, `npm run lint`, `npm test`.
- Manual: nova-dash → Espaço de Trabalho → Dashboard: trocar preset do `DateFilter` e o seletor de número; conferir Funil × Fluxo de Eventos Rápidos; aba Chatbot só aparece para a allowlist. Custos: "Atualizar agora" e ver erro/nota por serviço. Retenção: `GET /api/maintenance/retention` com Bearer `CRON_SECRET` devolve `{notificationsRead, notificationsOld, logs}`.

## Fronteiras
- **WhatsApp/bot** (produz os dados do funil): `closeCategory`/`closedAt`/tags/`botState` gravados em `app/_shared/lib/whatsapp/bot.ts`, `app/_actions/whatsapp/conversations.ts`, `app/_shared/lib/whatsapp/cron-tasks.ts`; `getContratadosTagCount` (`app/_actions/whatsapp/tags.ts`); `listWaNumberOptions` (`app/_actions/whatsapp/numbers.ts`); `listAllCreds` (`app/_shared/lib/whatsapp/numbers.ts`, custo WhatsApp); `fetchAdNames` (`app/_shared/lib/whatsapp/meta-ad-names.ts`).
- **Meta CAPI**: `reportLeadStageToMeta` é chamado por `bot.ts`, `conversations.ts` e `app/_actions/whatsapp/client-info.ts`.
- **Kanban/cards**: log `move` gravado em `app/_actions/cards/update-kanban.ts`; também lido por `app/_shared/lib/folder-report.ts`; UI `app/nova-dash/KanbanFlowPanel.tsx` e `app/nova-dash/minikanban.tsx`.
- **Logs**: `createLog`/`logWhatsAppEvent` em `app/_shared/lib/log.ts` (snapshot de setor no metadata); `/api/logs` serve o histórico da ficha.
- **Permissões**: `requireTeam`, `requirePermission`, `getSessionPermissions` (`app/_shared/lib/permissions-server.ts`); `isManager` (`app/_shared/lib/managers.ts`); `canViewChatbotDashboard` (`app/_shared/lib/chatbot-access.ts`).
- **Chatbot**: `app/nova-dash/workspace/chatbot/ChatbotPanel.tsx` embrulha `ChatbotDashboard` e segue o número/calendário do dashboard.
- **Setores/atividade pessoal**: `app/nova-dash/workspace/SectorDashboard.tsx` recebe o `DateRange` do `ManagerDashboard`; `app/nova-dash/FolderReport.tsx` também usa `DateFilter`; `app/_actions/users/get-my-activity.ts` aplica o mesmo peso de `dev_commit`; `app/_actions/sectors/get-sector-analytics.ts` NÃO aplica esse peso.
- **Automações**: `app/_shared/lib/automation-executor.ts` usa `appendSheetRow`.
- **Legado BotConversa**: `fetchEventsCount`, `fetchEventsByMonth`, `fetchBotconversaAll` em `app/_shared/lib/db/botconversa.ts`.
- **Workspace**: `app/nova-dash/workspace/Workspace.tsx` monta as seções `dashboard`/`gestao`/`custos`; `app/nova-dash/workspace/WorkspaceSidebar.tsx` esconde por permissão.
