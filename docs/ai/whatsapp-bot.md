# WhatsApp + Bot de IA — mapa para IA
> Verificado em 2026-09-23 · Escopo: `app/_shared/lib/whatsapp/**`, `app/api/whatsapp/**`, `app/_actions/whatsapp/**`, `app/nova-dash/workspace/{whatsapp,chatbot,numbers}/**`, `app/_shared/hooks/use-whatsapp.ts`, `app/nova-dash/instructions/**`, `app/api/{instructions,prompts}/**` + microserviço externo `D:\Chatbot_whatsapp` (`index.js`, `bot.js`, só leitura)

## TL;DR
- Inbox multi-número da WhatsApp Cloud API (Meta) + bot de triagem DPVAT/INSS. Webhook ingere → `bot.ts` monta o payload → microserviço "cérebro" (Express + Claude structured output, Railway) devolve `{reply|replies, action, state, memory, closeCategory, ...}` → o CRM executa (envia, fila, qualifica, encerra).
- O cérebro em produção = **instruções publicadas no banco** (`WhatsAppInstructions`) + playbook (`WhatsAppPlaybook`) + exemplos revisados, servidos por `/api/whatsapp/brain-prompt`. O `STATIC_SYSTEM_PROMPT` do `bot.js` é só fallback.
- Desde 23/09/2026 decisão de negócio é do cérebro: o código manda fatos (`conversationFacts`, `failCount`, `priorOutcome`) e mantém só redes de segurança (`sanitizeDecision`, `sendMutedFallback`, handoff em erro).
- Nenhuma falha do bot chega ao cliente como erro: tudo cai na fila humana (`status: "queued"`) com o motivo numa nota interna.
- O que mais quebra: (1) contrato CRM↔micro (enum de `state`/`closeCategory`, numeração R1..Rn, campo novo esquecido de um lado); (2) multi-número (contato gêmeo por linha, catálogo de template por WABA, número inativo); (3) anti-spam da Meta (opt-out/opt-in, cooldown, tetos da recuperação, marcapasso).

## Onde fica
| Caminho | Responsabilidade | Símbolos-chave |
|---|---|---|
| `app/api/whatsapp/webhook/route.ts` | GET handshake; POST valida HMAC, roteia por `phone_number_id`, ingere lote, bot 1x/contato, ficha IA, status | `GET`, `POST`, `verifySignature` |
| `app/api/whatsapp/brain-prompt/route.ts` | Serve instruções+playbook+exemplos ao micro (header `x-bot-secret`) | `GET` |
| `app/api/whatsapp/cron/{sla,nudge,recovery}/route.ts`, `app/api/whatsapp/cron/route.ts`, `app/api/whatsapp/cron/auth.ts` | 3 crons de 15min (vercel.json); a raiz roda as 3 (disparo manual) | `isCronAuthorized` |
| `app/api/whatsapp/messages/route.ts` | Thread paginada (`after`/`before`), poll do SWR | `GET` |
| `app/_shared/lib/whatsapp/service.ts` | Ingestão, dedup, reabertura, opt-out por regex, status de entrega | `ingestIncomingMessage`, `applyStatusUpdate`, `alertDeliveryFailure`, `isSilencedCloseCategory`, `whatsappChannelId`, `whatsappRecipients` |
| `app/_shared/lib/whatsapp/bot.ts` | Orquestra o cérebro e executa a decisão | `handleIncomingWhatsApp`, `sendBotReply`, `qualifyToQueue`, `postInternalNote`, `findLinkedCard` |
| `app/_shared/lib/whatsapp/client.ts` | Graph API da Meta | `sendText`, `sendMedia`, `sendVoiceNote`, `sendTemplate`, `sendReaction`, `markMessageRead`, `downloadMediaToS3`, `createMetaTemplate`, `fetchMetaTemplates`, `deleteMetaTemplate` |
| `app/_shared/lib/whatsapp/numbers.ts`, `crypto.ts` | Resolver multi-número (cache 60s) + token AES-256-GCM | `getCreds`, `getCredsByPhoneNumberId`, `getDefaultCreds`, `activeNumberConversationWhere`, `getInactiveNumberIds`, `ensureDefaultNumber`, `invalidateNumberCache`, `encryptSecret` |
| `app/_shared/lib/whatsapp/outbound.ts` | Envio de SISTEMA (automação, progresso, recuperação, assinatura) | `sendSystemWhatsApp`, `findOrCreateContactByPhone`, `isWindowOpen`, `normalizePhoneBR` |
| `app/_shared/lib/whatsapp/cron-tasks.ts`, `recovery-caps.ts` | Fases SLA / nudge / recovery; teto de provocações por número | `runSlaPhase`, `runNudgePhase`, `runRecoveryPhase`, `mergeResults`, `recoveryCapForPhoneNumberId` |
| `app/_shared/lib/whatsapp/opt-out.ts` | Regex de descadastro/reativação | `isOptOutMessage`, `isExactOptOutCommand`, `isOptInMessage`, `OPT_OUT_CONFIRMATION`, `OPT_OUT_FOOTER` |
| `app/_shared/lib/whatsapp/close-categories.ts` | Rótulos/menu de desfecho (módulo neutro, sem `use server`) | `CLOSE_CATEGORY_LABELS`, `CLOSE_CATEGORY_OPTIONS`, `QUALIFIED_BY_CATEGORY` |
| `app/_shared/lib/whatsapp/brain.ts`, `distill.ts`, `examples.ts` | Snapshot S3 + review; lição/playbook; bloco de exemplos | `captureConversation`, `readSnapshot`, `extractLesson`, `buildPlaybookDraft`, `publishPlaybook`, `getBrainExamples` |
| `app/_shared/lib/whatsapp/rule-events.ts` | Telemetria R1..Rn, recuperação, intervenções de código | `recordAppliedRules`, `recordCodeIntervention`, `recordRecoveryEvent`, `recordFollowupDecision`, `invalidatePlaybookCache` |
| `app/_shared/lib/whatsapp/review-tags.ts` | Vereditos e tags de erro da revisão (módulo neutro) | `REVIEW_VERDICTS`, `REVIEW_ERROR_TAGS`, `sanitizeErrorTags`, `CLOSED_REASON_LABELS` |
| `app/_shared/lib/whatsapp/instructions.ts` | Montagem/publicação das instruções | `renderInstructions`, `publishInstructions`, `parseRawPrompt` |
| `app/_shared/lib/whatsapp/flow-runner.ts`, `flow-tags.ts` | Fluxos pré-setados disparados pela IA | `listFlowsForBot`, `runFlowForContact`, `applyFlowTagsToContact` |
| `app/_shared/lib/whatsapp/assist.ts`, `ficha-ai.ts` | Agent-assist (micro) e ficha automática (Haiku direto) | `suggestReplyForContact`, `summarizeConversationToCard`, `transcribeMessageAudio`, `autoFillClientInfo`, `AI_FIELDS`, `FIELD_LABELS` |
| `app/_shared/lib/whatsapp/{status-notify,template-text,account-events,meta-ad-names}.ts` | Aviso de etapa do card; texto do template na thread; eventos de conta Meta; nomes de anúncio | `notifyStatusProgress`, `renderTemplateThreadText`, `handleAccountEvent`, `fetchAdNames` |
| `app/_actions/whatsapp/conversations.ts` | Lista/busca/atribuição/encerramento | `listWhatsAppConversations`, `getWhatsAppInboxVersion`, `searchWhatsAppConversations`, `assumeConversation`, `returnConversationToBot`, `closeConversation`, `markConversationUnread` |
| `app/_actions/whatsapp/send-message.ts`, `templates.ts` | Envio do atendente; catálogo/envio manual de template | `sendWhatsAppMessage`, `sendWhatsAppMedia`, `sendWhatsAppInternalNote`, `reactToWhatsAppMessage`, `sendWhatsAppTemplateMessage`, `syncWhatsAppTemplatesFromMeta`, `createWhatsAppTemplate` |
| `app/_actions/whatsapp/{instructions,reviews,rule-metrics}.ts` | Tela Revisão da IA (perm. `review_ai`) | `publishInstructionsDraft`, `submitReview`, `generatePlaybookDraft`, `approvePlaybook`, `getRuleMetrics` |
| `app/_actions/whatsapp/{numbers,contacts,close-reasons,client-info,client-documents,flows,tags,quick-replies,status-messages,assist}.ts` | Números, agenda, motivos `nq_*`, ficha, docs, fluxos, tags | `createWaNumber`, `importEnvNumber`, `listWaContactsDirectory`, `createCloseReason`, `CLIENT_FIELDS`, `getClientInfo`, `saveClientInfo`, `addClientFromConversation`, `saveWhatsAppFlow` |
| `app/_shared/hooks/use-whatsapp.ts` | SWR: versão 15s, lista 2min, thread 8s, não-lidas 30s | `useWhatsAppConversations`, `useWhatsAppMessages`, `useWhatsAppUnread` |
| `app/nova-dash/workspace/whatsapp/` | UI (inbox montado em `app/nova-dash/page.tsx`; Revisão da IA via `Workspace.tsx` seção `revisao-ia`) | `WhatsAppInbox`, `WhatsAppComposer`, `CopilotPanel` (aba Ficha = `FichaTab`), `AIReview`, `InstructionsPanel`, `PlaybookPanel`, `RuleMetricsPanel`, `ContactsDirectory`, `WhatsAppTemplatesModal`, `WhatsAppFlowsModal`, `media-rules.ts` (MIME aceitos pela Meta), `use-voice-recorder.ts` (ogg/opus), `wa-format.tsx` |
| `app/nova-dash/workspace/chatbot/ChatbotPanel.tsx`, `app/nova-dash/workspace/numbers/NumbersPanel.tsx` | Aba Chatbot do `StrategicDashboard`; tela Números (seção `numeros`) | `ChatbotPanel`, `NumbersPanel` |
| `app/nova-dash/instructions/**`, `app/api/instructions/**`, `app/api/prompts/**` | **NÃO são o cérebro**: "Locker" de instruções (model `Instruction`) e prompts do Roteiro (`SavedPrompt`, `ScriptTab.tsx`) | — |
| `D:\Chatbot_whatsapp\index.js` | Rotas do micro (todas, menos `/health`, exigem `x-bot-secret` = env `BOT_SECRET`) | `/reply`, `/farewell`, `/followup-decision`, `/recovery-message`, `/suggest`, `/summarize`, `/transcribe`, `/extract-contract-data`, `/confirm-contract-data`, `/distill-lesson`, `/consolidate-playbook`, `/health` |
| `D:\Chatbot_whatsapp\bot.js` | Prompt, schema e decisão | `decide`, `STATES`, `responseSchema`, `STATIC_SYSTEM_PROMPT`, `getStaticPrompt`, `renderPlaybook`, `buildDynamicContext`, `authorTag` |

## Fluxo principal
1. **Webhook** (`POST /api/whatsapp/webhook`): `verifySignature` com `WHATSAPP_APP_SECRET`. `change.field !== "messages"` → `handleAccountEvent(field, value, entry.id)` (template status, qualidade, violação). `metadata.phone_number_id` → `getCredsByPhoneNumberId`; desconhecido → `reportCriticalError` e o lote é ignorado. O resolver só carrega números `active`, então inbound de número desativado também é descartado (salvo se for o das envs). A rota tem `maxDuration` 120 e roda o bot inline.
2. **Ingestão** (`ingestIncomingMessage`): contato por `(phone, numberId | null)` adotando legado; `optedInAt` na 1ª inbound; `referral` first-touch; upsert da conversa zerando `botNudge30At`; `closed` → `bot` (exceto opt-out e `isSilencedCloseCategory`; zera `recovery*`; limpa `botMemory/botState` se não qualificado e >7d); `standby` → `bot` (`recoveryOutcome: "recuperado"`); dedup por `waMessageId` (retry → `null`); mídia → S3; grava `direction: "in"`; `broadcastToRelay` no canal `whatsapp:<contactId>`; opt-out por regex; `importSource === "botconversa"` → `queued`; notificação só em `queued`/`human`.
3. Webhook chama `handleIncomingWhatsApp` **uma vez por contato** (última msg nova em status `bot`), depois `autoFillClientInfo`, depois `applyStatusUpdate` (status nunca regride; `failed` → `alertDeliveryFailure`). Falha de ingestão → HTTP 500 (Meta reenvia); falha depois de persistir → 200.
4. **Bot** (`handleIncomingWhatsApp`): `markMessageRead(typing)` → dorme `BURST_DEBOUNCE_MS` (8s) → desiste se chegou inbound mais nova → confere que segue `bot` → lote (inbound desde o último out não-interno, até 12) → `mediaList` com URL pré-assinada 600s (áudio já transcrito vira texto) → nota "CRUZOU" se a msg cruzou com o último out → assinatura (`handleSignatureClientReply`/`activeCycle`, import dinâmico) → payload.
5. Payload de `/reply`: `numberId` (o micro ainda ignora), `contact, processInfo` (via `findLinkedCard`), `flows` (só com descrição), `history` (30, `role` client|bot|agent|system + `source` de `SYSTEM_SOURCE_LABELS`, sem nome de atendente), `message, media, mediaList, memory, state, failCount, priorOutcome{qualified,closeCategory}, conversationFacts{docsReceived,registeredClient}, business, signature`, e `lookupResult` na 2ª passada.
6. `callBrain` → `POST {CHATBOT_URL}/reply` (timeout 45s; até 3 tentativas só em timeout; 1 retry em outro erro) → `sanitizeDecision`. `action: "lookup"` → `runLookup` (status_processo | dados_cadastro | documentos_enviados) e 2ª chamada. `continue` vazio sem `silent` → 1 retry com nota de sistema. Persiste `transcripts` → desiste se chegou inbound nova → grava `botMemory/botState/botFailCount` → envia `replies` (só em `SCRIPT_STATES`) ou `reply` via `sendBotReply` → executa a ação → log `wa_bot` (+`usage`, `appliedRules`, `leaked`) → `recordAppliedRules`. `catch` → `handoffToQueue` com o motivo.
7. **Ações**: `continue` (sem texto e sem `silent` → fila) · `qualify` (`maybeStartSignatureFlow`; se `"queue"` → `qualifyToQueue`: `queued` + `qualified=true` + tag "Qualificada"; só na 1ª qualificação: notificação + `reportLeadStageToMeta` + `recordSectorTask` se ainda não há card) · `disqualify` (`disqualifyAndClose`, aceita `nq_*`) · `handoff` (`handoffToQueue`, grava `closeCategory`) · `resolve` (`resolveAndClose`, default `perguntas`) · `send_flow` (`runFlowForContact`; falhou sem texto → fila). `optOut: true` só envia confirmação ensinando SAIR e encerra.
8. **Micro**: `getStaticPrompt` busca `{BRAIN_PROMPT_URL}/api/whatsapp/brain-prompt` (cache `BRAIN_TTL_MS` 5min; falha → texto em memória ou `STATIC_SYSTEM_PROMPT`) → system = [bloco estático com `cache_control`, `buildDynamicContext`] → `responseSchema` (1º campo `rationale`, nunca enviado) → devolve decisão com `usage`, `leaked`, `transcripts`.
9. **Crons** (`*/15` no `vercel.json`): `sla` (fila em degraus 10m/1h/4h/24h, humano 30m/2h/12h, `sent` >12h sem `delivered`, cards estourados; lembretes de assinatura desligados por `SIGNATURE_CRON_ENABLED = false`) · `nudge` (30min de silêncio após pergunta do bot, só com janela de 24h aberta → `/followup-decision`; +60min → `/farewell` e `standby` ou `closed`) · `recovery` (`standby` vencido → `/recovery-message` texto livre na janela; janela fechada cai no template `recuperacao_triagem_1`; a última tentativa usa `recuperacao_triagem_final`). Fases com envio passam pelo marcapasso (`createPacer`, orçamento 240s por rodada).
10. **Atendente**: UI → server action → `client.ts` com `contact.numberId` → `persistOutbound` (conversa vira `human` + `assignedToId`) → relay. Client: `getWhatsAppInboxVersion` a cada 15s e a lista só recarrega quando o hash muda; SSE (`useChatStream`) acelera.

## Dados
- `WhatsAppNumber` (`whatsapp_numbers`): `accessTokenEnc` (chave `WHATSAPP_CRED_KEY`, fallback `NEXT_AUTH_SECRET`); `active=false` = linha somente leitura; `templatesPaused` pausa só template; `isDefault` = envio de conversa sem `numberId`; `appSecretEnc` existe mas **não é lido** (HMAC usa `WHATSAPP_APP_SECRET` global). Sem linha nenhuma → envs `WHATSAPP_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID`.
- `WhatsAppContact`: `@@unique([numberId, phone])` — o mesmo telefone é **um contato por linha** (com `numberId` null a unique não barra duplicata: no Postgres NULL é distinto); `userId`/`processId` sem FK; `optedOut` (só o gêmeo criado por `findOrCreateContactByPhone` herda; o criado pelo webhook nasce sem), `optedInAt/optInSource` (inbound|manual); `clientDraft`/`draftDocuments` (ficha antes de virar card); `ad*`/`ctwaClid` first-touch; `aiFilledFields`, `hospitalHint`; `importSource = "botconversa"` nunca cai no bot.
- `WhatsAppConversation` (1:1 com contato): `status` bot|queued|human|standby|closed; `qualified` Boolean? (null ≠ false); `closeCategory` também é gravado em `queued` por handoff/qualify; `closedAt` = último encerramento; `botMemory`/`botState`/`botFailCount`; `botNudge30At` (o `botNudge24At` só é zerado); `recoveryAttempts/NextAt/Outcome` (recuperado|esgotado|opt_out|bloqueado); `queuedAt/queueAlertAt`, `deliveryAlertAt`; `lastReadAt` legado (epoch = "não lida" manual); `urgent` = coluna morta.
- `WhatsAppMessage`: `direction` in|out; `internal` = nota só da equipe; `sentByBot`; `authorId` = humano; `systemSource` (recovery|automation|progress|signature_otp|signature_reminder|signature_resend) marca proativa e alimenta o cooldown; `waMessageId` unique (dedup); `transcript`; `reaction`; `editedAt/deletedAt` são locais.
- `WhatsAppTemplate`: `@@unique([numberId, name])`; `status` APPROVED|PENDING|REJECTED|PAUSED|DISABLED; `category` UTILITY|MARKETING|AUTHENTICATION; `headerFormat` + `headerMediaKey` (S3 `whatsapp/templates/`).
- Cérebro: `WhatsAppInstructions` e `WhatsAppPlaybook` (rascunho|publicado|descartado; vale a maior `version` publicada); `WhatsAppReview` (`closedReason` bot_disqualify|bot_resolve|cron_silencio|manual; `s3Key` em `whatsapp-brain/conversas/`); `WhatsAppRuleEvent` (`kind` rule|followup|recovery|code; `ruleId` R#/T#; texto é snapshot). S3: `whatsapp-brain/instructions/current.json`, `whatsapp-brain/playbook/current.json`.
- Outros: `WhatsAppCloseReason` (motivos `nq_*` criados pela equipe), `WhatsAppFlow` (`name` unique; `description` é o gatilho da IA; `steps`, `tagIds`), `WhatsAppTag`/`WhatsAppConversationTag` (`createdAt` = data da aplicação; "Qualificada" e "Contratados" medem o funil em `bot-funnel.ts`), `WhatsAppConversationRead`, `WhatsAppQuickReply`, `StatusMessageConfig`.
- `closeCategory`: enum do micro = qualificado, nao_qualificado, perguntas, novo_acidente, transferido, `nenhum` (vira null), nq_acidente_muito_antigo, nq_sem_qualidade_de_segurado, nq_sem_lesao_pertinente, nq_ja_recebe_beneficio_inss, nq_ja_tem_acao_advogado, nq_desistiu. O estático do CRM tem outros `nq_*` + contratado_perdido, sem_resposta, descartado. Rótulo de `nq_*` fora do estático vem de `whatsapp_close_reasons` (existência das chaves do micro lá: não verificado). O código testa sempre o prefixo `nq_`.
- `STATES` (bot.js): saudacao, coleta_nome, triagem_quando_onde, triagem_hospital, triagem_lesao, triagem_inss, script_beneficio_1..3, script_honorarios, script_fechamento, pergunta_interesse, coleta_documentos, validacao_documentos, contornando_objecao_1/2, encerrando.
- Env do CRM (só nomes): `WHATSAPP_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_WABA_ID`, `WHATSAPP_API_VERSION`, `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_APP_SECRET`, `WHATSAPP_CRED_KEY`, `WHATSAPP_TEST_NUMBERS`, `WHATSAPP_SYSTEM_COOLDOWN_HOURS`, `WHATSAPP_STATUS_TEMPLATE`, `WA_RECOVERY_DAILY_CAP`, `WA_SEND_GAP_MIN_S`/`MAX_S`, `CHATBOT_URL`, `CHATBOT_URL_STAGING`, `CHATBOT_SECRET`, `CLAUDE_API_KEY` (ficha IA direto na Anthropic), `CRON_SECRET`, `AWS_*`. Env do micro: `BOT_SECRET`, `BRAIN_PROMPT_URL`, `BRAIN_TTL_MS`, `ANTHROPIC_API_KEY`, `GOOGLE_API_KEY` (transcrição Gemini), `MODEL`, `MODEL_SMALL`, `SUGGEST_MODEL`, `TRANSCRIBE_MODEL`, `MODEL_DISTILL`, `MODEL_PLAYBOOK`.
- Logs (`logWhatsAppEvent`): `wa_bot`, `wa_close`, `wa_assign`, `wa_reopen`, `wa_return_bot`, `wa_text`, `wa_template`, `wa_flow`, `wa_note`, `wa_reaction`, `wa_suggest`, `wa_summary`, `wa_transcribe`, `wa_ficha_ai`, `wa_account`, `wa_review`, `wa_contact`.

## Regras invioláveis e armadilhas
- **Produção lê as instruções do banco.** Editar só o `STATIC_SYSTEM_PROMPT` do `bot.js` não muda nada enquanto `BRAIN_PROMPT_URL` responder. Publicar = `publishInstructions` (S3 antes do banco). `brain-prompt` lê do banco; o `current.json` do S3 é só cópia. O micro rejeita `rendered` com menos de 500 caracteres e fica no texto anterior ou no fallback.
- **State/closeCategory/action novo = 3 lugares**: instruções (banco), `STATES`/`responseSchema` no `bot.js` e `BotDecision`/`switch` no `bot.ts`. Valor fora do enum do structured output silencia a instrução.
- **Campo novo no payload** também precisa entrar no destructuring do `/reply` em `index.js` (o `priorOutcome` já se perdeu assim) e em `decide`/`buildDynamicContext`.
- **Deploy do micro (Railway) ANTES do CRM (Vercel)** quando o contrato muda. Um micro antigo ignora campos novos sem erro.
- **R1..Rn** é reconstruída em 3 lugares: `renderPlaybook` (bot.js), `flattenRules` (rule-events.ts) e o laço de `getRuleMetrics` (`app/_actions/whatsapp/rule-metrics.ts`). Mudar a numeração num só quebra as métricas.
- **Bytes estáveis** em `brain-prompt`, `renderInstructions` e `getBrainExamples` (sem timestamp nem ordem instável): o cache de prompt da Anthropic é por prefixo.
- **`captureConversation` ANTES do update que encerra**, passando `outcome` (`closeCategory`/`qualified`) quando o desfecho muda nesse mesmo update (o cron, `finalizeClose`, omite e usa o do banco). Bot e cron preservam `botMemory/botState` (a limpeza é na reabertura, `STALE_CONTEXT_MS` 7d). `closeConversation` manual e o opt-out por regex (`service.ts`) zeram a ficha.
- **Não reintroduza trava de negócio no código** (ex.: "não entendeu 2x → fila", "resolve com card/docs → fila", removidas em 23/09). Mande o fato no payload e a regra nas instruções.
- **`sanitizeDecision` espelha o micro** (`looksLikeReasoning`, `SCRIPT_STATES`, `SCHEMA_TOKENS`): mudou lá, mude aqui. `replies` fora de `SCRIPT_STATES` é descartado.
- **Nome do atendente não vai ao cérebro** (o bot passou a citá-lo). Mensagem proativa nova precisa de `systemSource` + entrada em `SYSTEM_SOURCE_LABELS`, senão entra no histórico como fala do bot.
- **Opt-out só por regex**: `isExactOptOutCommand` vale sempre; frase ambígua só fora do modo bot; `decision.optOut` NÃO marca `optedOut`. Erro 131050 da Meta marca `optedOut`. O "de" é obrigatório em "para de enviar" ("dá para enviar" era falso positivo). "parar de receber o BPC" ainda casa.
- **Número inativo**: `getCreds(numberId)` devolve null e nunca cai no default. Crons filtram por `activeNumberConversationWhere()`, cujo OR com `numberId: null` é proposital (`notIn` sozinho derruba o legado). Inbox mostra `readOnly` no DTO. O cache de credenciais é de 60s por instância: `invalidateNumberCache` só limpa a instância atual.
- **Tempo do webhook**: o bot roda dentro do `POST` (`maxDuration` 120). O pior caso (8s de debounce + 3×45s de timeout do cérebro) passa disso, e a função morre antes do `handoffToQueue`. Não aumente timeout/tentativas sem rever esse teto.
- **`sendBotReply` descarta em silêncio** texto idêntico ao último `out` dos últimos 10min e qualquer envio a contato `optedOut` (não lança erro).
- **`runFlowForContact` não checa opt-out nem janela de 24h** (manda `sendText`/`sendMedia` direto). Chame só dentro de conversa viva.
- **Multi-número**: todo envio usa o `numberId` do contato. `sendSystemWhatsApp` recebe `contactId` (ou `numberId`) de quem sabe; sem isso a linha é deduzida pelo dono do template. Upsert por `phone` ou `name` precisa de find-or-create manual (uniques compostas).
- **Template**: o catálogo é por WABA; todo envio exige `status === "APPROVED"`; `templatesPaused` barra em `sendTemplate` (ponto único). Desativar número não é o mesmo que pausar template.
- **Anti-spam** em `sendSystemWhatsApp`: opt-out, cooldown `WHATSAPP_SYSTEM_COOLDOWN_HOURS` (6h, exceto `transactional`); fora da janela de 24h exige `optedInAt` + template.
- **Recuperação**: teto por número em `recovery-caps.ts` (padrão 4), teto diário `WA_RECOVERY_DAILY_CAP` (60), marcapasso `WA_SEND_GAP_MIN_S/MAX_S` (7–15s), horário 7h–21h BRT, `standbyBlockReason`.
- **Brechas conhecidas** (não "conserte" sem pedido): `sendWhatsAppTemplateMessage` manual não checa opt-in/cooldown; reabrir `closed` zera `recoveryAttempts`; `OPT_OUT_FOOTER` desligado (import comentado); erro 131049 não tratado; templates de recuperação são MARKETING na Meta (auditoria de 24/08, categoria não está no código).
- **Silêncio pós-desqualificação**: `nao_qualificado`, `nq_*` e `descartado` não reabrem pro bot (`isSilencedCloseCategory`); reabrir é pelo botão do inbox.
- `assumeConversation` **preserva `qualified`**; `returnConversationToBot` zera `botNudge30At`. Devolver ao bot faz a IA retomar a triagem e é a maior fonte de desqualificação de lead já atendido.
- **Inbox**: nunca use `distinct` do Prisma com `orderBy` em `whatsapp_messages` (puxa tudo e deduplica em memória). Use `LATERAL`/`DISTINCT ON`, como em `loadConversations`. A lista é capada em 1000 (`LIST_PAGE`); fora dela só a busca no servidor acha.
- **Fuso**: cortes de dia/mês via `app/_shared/utils/date-br.ts` (a Vercel roda em UTC).
- **Custo de IA**: toda chamada nova grava `metadata.usage` no log (`get-ai-corner.ts` soma por `jsonb_exists`).
- **Ficha por IA quase nunca pula a chamada**: o webhook roda `autoFillClientInfo` para todo contato com inbound novo, em qualquer status (`fichaCandidates`), e o atalho "A ficha já está completa." só vale com TODOS os `AI_FIELDS` preenchidos, inclusive os opcionais `telefone_secundario`/`rede_social` (o bot não pede, quase sempre vazios). Na prática o Haiku roda a cada lote, com até 4 anexos. Campo novo em `AI_FIELDS` só piora.
- **Ficha editada pelo Copiloto não entra no histórico do card**: `saveClientInfo` não chama `createLog` nem `logWhatsAppEvent`. No card só aparecem `ficha_ai_fill` (`FICHA_AI_ACTION`, preenchimento da IA) e o `create` de `addClientFromConversation`.
- **O cérebro não vê a ficha**: o `/reply` leva só `contact` (nome do perfil + telefone) e `processInfo` (nome/etapa/serviço do card); `runLookup` com `dados_cadastro` devolve só `{cadastrado, nome}`. Campo de ficha não mexe no micro.
- `fichaComplete` do DTO (`isFichaComplete` em `loadConversations`: nome + CPF + endereço) é calculado, mas nenhuma tela lê: o inbox não tem selo de ficha incompleta.
- **Server action que dá `throw` aparece mascarada em produção**, então o inbox mostra erro genérico.
- **Migrations**: nunca `prisma migrate dev` (drift do Neon quer resetar produção). Use `migrate diff` + `db execute` + `migrate resolve`.
- **PTT (voz)** exige ogg/opus + upload em `/media` + `voice: true`; só `sendVoiceNote` faz isso.
- **Fluxo para a IA**: só aparece para ela se tiver `description`, e o `flowName` tem que ser exato (`findUnique` por `name`).
- `review_ai` (Instruções/Playbook/Revisão) é travado por e-mail além da permissão (`isAiReviewer`, env `AI_REVIEW_EMAILS`); a aba Chatbot usa `canViewChatbotDashboard`.
- **Auth das actions**: `conversations.ts`, `send-message.ts`, `templates.ts`, `flows.ts`, `tags.ts` etc. usam um `requireTeamMember` local (só role ADMIN*), que **não aplica a trava de IP** do `requireTeam`. `requirePermission`/`requireTeam` só em `instructions`, `reviews`, `rule-metrics`, `numbers` e parte de `contacts`. `status-messages.ts` exige só sessão (qualquer logado, cliente incluso, chega em `resendStatusMessage`). `app/api/instructions/**` e `app/api/prompts/**` também não checam role. Em código novo use `permissions-server.ts`; não copie esses padrões.

## Receitas
- **Mudar o comportamento do bot** → rascunho em Revisão da IA → Instruções (`upsertSection`, `publishInstructionsDraft`) · cuidado: vai ao ar em ≤5min (TTL do micro) · valide com número de teste (`CHATBOT_URL_STAGING` + `WHATSAPP_TEST_NUMBERS`) e o log `[BRAIN] prompt carregado` no Railway.
- **Novo state/closeCategory/action** → `STATES`/`responseSchema` + fallback no `bot.js`, instruções, `BotDecision` + `switch` no `bot.ts` (e `close-categories.ts` ou `WhatsAppCloseReason`; `nq_*` novo só vai à CAPI se entrar em `META_LEAD_STAGES`) · deploy do micro primeiro · valide `metadata` do log `wa_bot`.
- **Novo fato para a IA** → `basePayload` em `handleIncomingWhatsApp` + destructuring do `/reply` (`index.js`) + `buildDynamicContext` · cuidado: não quebrar o cache (vai no bloco dinâmico, nunca no estático).
- **Nova mensagem automática** → `sendSystemWhatsApp({ contactId, source, templateName, ... })` + label em `SYSTEM_SOURCE_LABELS` · cuidado: template da WABA certa e opt-in · valide pelo log `wa_text`/`wa_template` (os "não enviou" trazem o motivo).
- **Novo template** → `createWhatsAppTemplate` com `numberId` (nasce PENDING; a Meta exige exemplo por variável) → aprovação chega por `message_template_status_update` (`handleAccountEvent`) · valide com `syncWhatsAppTemplatesFromMeta`.
- **Novo caminho de encerramento** → `captureConversation` antes, depois `status: "closed"`, `closedAt`, zerar `recovery*`/`queuedAt`, coerência com `QUALIFIED_BY_CATEGORY` · a tag automática de desfecho (`syncCloseTag`) só existe no `closeConversation` manual · valide a review na fila (`listReviews`).
- **Cron/recuperação** → `cron-tasks.ts` + `recovery-caps.ts` (a pill "Nª de N" do inbox lê o mesmo mapa) · disparo manual: `GET /api/whatsapp/cron` com `CRON_SECRET` (`whatsapp-cron.cmd`) · valide `[WHATSAPP CRON]` e eventos `kind: "recovery"`/`"code"`.
- **Novo número** → tela Números (`createWaNumber` valida o token na Meta) · webhook descarta `phone_number_id` desconhecido · `ensureDefaultNumber` adota legado.
- **Novo endpoint de IA no micro** → rota em `index.js` com `x-bot-secret` + função no `bot.js` + chamador no CRM (`CHATBOT_URL`/`CHATBOT_SECRET`, timeout, fallback, `metadata.usage`).
- **Campo novo na lista do inbox** → `loadConversations` (SQL `LATERAL`) + DTO `WhatsAppConversationDTO` + hash de `getWhatsAppInboxVersion` se a mudança precisar aparecer sem esperar 2min.
- **Nova consulta da IA ao banco** → `runLookup` (só dado não sensível) + enum `lookup` no `responseSchema`.
- **Campo novo na ficha do contato** → três listas mudam juntas:
  - `AI_FIELDS` + `FIELD_LABELS` em `ficha-ai.ts` (só se a IA deve extrair; `email`, `hospital` e `obs` ficam fora de propósito);
  - `CLIENT_FIELDS` em `app/_actions/whatsapp/client-info.ts` (o tipo `ClientInfoFields` deriva dela; `getClientInfo`, `saveClientInfo` e `addClientFromConversation` passam a ler/gravar o campo);
  - `FField` + `setField`/`byAi` no `FichaTab` de `app/nova-dash/workspace/whatsapp/CopilotPanel.tsx`.
  - cuidado: a ficha grava texto direto no `User` com o MESMO nome de coluna → coluna `String?` no `User` (e no `Process`, pela receita do card) via `/migration`, aplicada ANTES do deploy (sem ela a ficha, a IA e o "Adicionar cliente" quebram). Data/número não serve: `currentFields` faz `.trim()` e `sanitizeFields` grava string. A regra DATAS do prompt de `autoFillClientInfo` aceita só o ano ("2019") ou mês/ano: campo que exige data completa precisa de exceção ali (a UI marca parcial com `partialDate`). Não exige deploy do micro.
  - depois: a parte do card segue "Campo novo no card" de `docs/ai/kanban-cards.md`.
  - valide: preencher pelo Copiloto → "Adicionar cliente" → campo no card; botão de IA do Copiloto (`fillClientInfoWithAI`) devolve o `reason`; log `wa_ficha_ai`.

## Testes e validação
- Cobertura automática é mínima: `tests/whatsapp-template-text.test.ts` (`renderTemplateThreadText`), `tests/whatsapp-wa-format.test.ts` (`stripWaMarkup`), `tests/date-br.test.ts`, `tests/permissions.test.ts`. Não há teste de `bot.ts`, `service.ts`, `cron-tasks.ts` ou `opt-out.ts`.
- `tests/signature-templates.smoke.test.ts` **cria templates reais na Meta** (só roda com `npm run sign:templates`).
- Gate (igual ao CI `.github/workflows/ci.yml`): `npx tsc --noEmit`, `npm run lint`, `npm test`. O `next build` local morre por OOM; deixe o build para a Vercel.
- Manual: número de teste → cérebro de staging; logs Vercel `[WHATSAPP BOT]`/`[WHATSAPP CRON]`/`[WA NUMBERS]`; Railway `[BOT]`, `[BRAIN]`, `[BOT] VAZAMENTO:`; `GET /health` do micro; log `wa_bot` (`outcome`, `hasAppliedRulesField`, `leaked`, `usage`); aba Métricas (`RuleMetricsPanel`).

## Fronteiras
- **Kanban/cards**: `findLinkedCard` (vínculo manual `userId`/`processId` do contato; senão últimos 8 dígitos em `User`/`Process`) + `getStatusLabel`/`getStatusDescription` (`app/nova-dash/card-dialog/constants.ts`); `notifyStatusProgress` ← `app/_actions/users/update-users.ts`, `app/_actions/process/update-process.ts`; `addClientFromConversation` cria o card.
- **Automações**: `app/_shared/lib/automation-executor.ts` → `sendSystemWhatsApp` (`source: "automation"`).
- **Assinatura eletrônica**: `app/_shared/lib/signature/core.ts` (`handleSignatureClientReply`, `activeCycle`, `maybeStartSignatureFlow`, env `SIGNATURE_AUTO_ENABLED`; chama `/extract-contract-data` e `/confirm-contract-data`), `app/_shared/lib/signature/otp.ts`, `app/_actions/signature/contracts.ts`.
- **Auth**: `app/_actions/auth/password-reset.ts` usa `sendText` sem `numberId` (número default, exceção intencional).
- **Analytics**: `app/_actions/analytics/get-chatbot-analytics.ts`, `bot-funnel.ts`, `get-ai-corner.ts`.
- **Meta CAPI**: `reportLeadStageToMeta` (`app/_shared/lib/meta-conversions.ts`).
- **Infra compartilhada**: `broadcastToRelay` (`chat-relay.ts`, env `CHAT_RELAY_URL`) + `useChatStream` (`use-chat.ts`); `logWhatsAppEvent` (`log.ts`); `reportCriticalError`; `recordSectorTask` (`sector-tasks.ts`); `requirePermission` (`review_ai`, `manage_wa_contacts`, `manage_wa_numbers`) em `permissions-server.ts`; `middleware.ts` libera webhook/cron/brain-prompt em `PUBLIC_API_PREFIXES`.
- **Segredo CRM↔micro**: `CHATBOT_SECRET` (CRM) = `BOT_SECRET` (micro), nos dois sentidos (`/reply` etc. e `/api/whatsapp/brain-prompt`).
